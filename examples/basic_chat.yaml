case_id: basic_chat_001

backend:
  provider: mock

protocol:
  type: chat_completions

model: mock-model

features:
  streaming: false
  tool_calling: false
  structured_output: false

messages:
  - role: user
    content: "Explain what Agent Eval Lab is in one sentence."

expected:
  contains_text: "Agent Eval Lab"

metadata:
  purpose: "Validate that the case loader can parse a minimal chat case."
  mock_response: "Agent Eval Lab is an Agentic LLM Systems behavior analysis framework."