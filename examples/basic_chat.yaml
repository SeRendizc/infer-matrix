case_id: basic_chat_001

backend:
  provider: mock

protocol:
  type: chat_completions

model: mock-model

features:
  streaming: false
  tool_calling: false
  structured_output: false

messages:
  - role: user
    content: "Explain what InferMatrix is in one sentence."

expected:
  contains_text: "InferMatrix"

metadata:
  purpose: "Validate that the case loader can parse a minimal chat case."
  mock_response: "InferMatrix is an Agentic LLM Systems behavior analysis framework."